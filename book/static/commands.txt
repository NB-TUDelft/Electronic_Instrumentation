conda env create -f "
conda activate NB2214
python -m alpaca_kernel install
python -m ipykernel install

==uninstall==
jupyter kernelspec uninstall alpaca -y && jupyter kernelspec uninstall python3 -y
